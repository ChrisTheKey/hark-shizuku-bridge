<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- MainActivity handles all foreground OACP actions via activity intent filters.
             Hark dispatches type="activity" with startActivity() from the foreground. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="${applicationId}.oacp.ACTION_INCREMENT_COUNTER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="${applicationId}.oacp.ACTION_DECREMENT_COUNTER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="${applicationId}.oacp.ACTION_SET_COUNTER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="${applicationId}.oacp.ACTION_RESET_COUNTER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- BroadcastReceiver handles background actions — no UI needed -->
        <receiver
            android:name=".oacp.OacpActionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="${applicationId}.oacp.ACTION_GET_BATTERY" />
            </intent-filter>
            <intent-filter>
                <action android:name="${applicationId}.oacp.ACTION_GET_COUNTER" />
            </intent-filter>
        </receiver>

    
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <receiver
            android:name="com.christhekey.harkbridge.oacp.OacpActionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="${applicationId}.oacp.ACTION_HOME"/>
                <action android:name="${applicationId}.oacp.ACTION_BACK"/>
            </intent-filter>
        </receiver>

</application>


</manifest>
